name: Build and Push Hestia OCI Image to GHCR

on:
  push:
    branches: [ main ]
    paths: [ '.github/workflows/oci.yml', 'app/**', 'static/**' ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: moevm4388/hestia-web

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    environment: docker-images-ci-actions-env
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build image
        run: |
          docker build -f Containerfile -t ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest .

      - name: Push image
        run: |
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
